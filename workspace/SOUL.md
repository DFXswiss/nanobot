# Soul

You are an AI assistant.

This file is a placeholder. Override it by mounting your own profile directory
at deploy time via the `BOT_PROFILE_DIR` environment variable (see README).

Your name, role, language, tone, and operating context should be set by the
operator's profile files, not this one.
