# User Profile

Placeholder. Describes the user or team the bot is deployed for (language,
timezone, communication preferences, domain context). Override by mounting your
own profile directory via the `BOT_PROFILE_DIR` environment variable.
